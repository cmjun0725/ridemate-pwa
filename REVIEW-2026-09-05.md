# RideMate 전체 리뷰 및 수정 — 2026-09-05

## 수정한 문제

| 영역 | 근거 파일 / 함수 | 문제와 조치 |
| --- | --- | --- |
| 프로필 | `functions/src/index.ts` / `saveRiderSettings` | 온보딩 완료만 저장해도 비상연락처와 알림 값이 초기화됨. 전달한 필드만 병합하고 인증·운영 상태는 보존 |
| 권한 | `firestore.rules` | 본인 프로필에 인증 관련 임의 필드 기록 가능, 다른 참여 기록 조회 가능, 푸시 토큰 직접 변경 가능. 앱이 이미 사용하는 서버 저장 경로로 제한 |
| 라이딩 기록 | `leaveRide` | 진행·완료 후 참여 취소 가능. 모집 단계에서만 허용 |
| 노쇼 입력 | `castNoShowVote` | boolean 외 입력도 저장 가능. 명시적 boolean 검증 |
| 알림 | `notifyVoteOpened` | 첫 10명에게만 전송하고 안전 알림 설정 무시. 전체 참여자 설정 확인 후 나누어 전송 |
| 코스 생성 | `createRidePlan`, `recommendRoute` | 정원 정수·개인 일정·경로 좌표 및 한국 좌표 범위 검사 보완 |
| 상세 화면 | `getRideDetails` | 방장 정보 누락, 숨김 상태 검사 누락 보완 |
| 거리 | `src/App.tsx` / `submitManual` | 임의 입력 거리와 지도 경로가 불일치할 수 있어 계산된 거리로 저장 |
| 작성 데이터 | `CreateRide` | 추천 저장 시 현재 작성 데이터를 명시적으로 전달 |
| 날짜 | `src/domain.ts` | 존재하지 않는 날짜·잘못된 시간 거부. 상세 화면 잘못된 날짜는 일정 미정 표시 |
| 알림 이동 | `App` | 고정 100ms 대기 중 프로필 로딩이 늦으면 이동 실패. 대상 렌더링 후 이동·포커스 처리 |
| 프로필 오류 | `ProfileTools` | 로딩 실패 시 영구 스켈레톤 대신 오류·재시도 제공 |
| 업데이트 | `src/main.tsx` | 서비스워커 교체 시 강제 새로고침으로 작성 데이터 손실 가능. 자동 새로고침 제거 |
| 오프라인 | `public/sw.js` | 캐시 적중해도 불필요한 네트워크 요청으로 오프라인 오류 발생. 캐시 적중 시 즉시 반환, 응답 복제 시점 수정 |
| 캐시 / 링크 | `public/sw.js` | 다른 캐시 삭제 방지, 알림 링크 동일 출처 제한 |
| 접근성 | `src/styles.css` | 주소 줄바꿈, 알림 제목 상단 여백, 동작 줄이기 설정 대응 |

## 검증

- 프런트 / 서버 lint 및 TypeScript 프로덕션 빌드 통과.
- 기존 정책 테스트 및 잘못된 날짜·시각·윤년 회귀 테스트, 총 6개 통과.
- 운영 URL을 브라우저에서 열어 홈 / 주요 메뉴의 초기 렌더링 확인.
- 인증 후 실제 참여·투표·FCM 수신, iOS / Android 실기기 및 Firestore Emulator 권한 테스트는 이번 확인 범위에 포함되지 않음.

## 추가 검토가 필요한 부분

- 노쇼 확정은 현재 최소 표수·과반 달성 즉시 기록됨. 24시간 종료 시 최종 집계를 원한다면 확정 정책과 예약 집계를 함께 변경해야 함.
- 공개 라이딩 조회는 60개를 가져온 뒤 종료·과거 데이터를 필터링함. 데이터 증가 시 앞으로 예정된 라이딩을 먼저 조회하도록 인덱스와 페이지 처리 개선 필요.
- 장소가 확정되지 않은 상태의 저장은 차단하지만, 지도 SDK 네트워크 실패와 검색 결과 없음 안내는 추가 개선 여지가 있음.
- 직접 입력 코스는 생성 전 장소 변경 가능. 저장 후 전체 경로 편집 및 참여자 변경 안내는 별도 기능임.

이번 배포를 모든 환경의 오류가 없다는 보증으로 간주하지 않으며, 위 미검증 항목은 완료로 표시하지 않음.
